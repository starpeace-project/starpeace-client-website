language: node_js
node_js:
    - "node"

before_install:
  - npm install -g grunt-cli

install:
    - npm install
    - npm rebuild node-sass

cache:
  directories:
    - node_modules

script:
  - npm run generate
